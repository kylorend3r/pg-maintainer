# Rust build artifacts
target/
Cargo.lock

# Log files produced by the tool
*.log
logs/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment files
.env
.env.*

# Config files with real credentials (keep config.example.toml)
config.toml

# Claude Code project guidance (local dev only)
CLAUDE.md

# Local test-fixture schema/DML (not part of the shipped tool),
# except the DDL fixture CI needs to set up the integration-test database
schema/*
!schema/setup_test_schema.sql

# Design docs / planning scratch (not part of the shipped tool)
plans-designs/

# Claude Code local settings
.claude/
